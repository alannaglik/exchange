<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd
		http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <changeSet author="Utworzenie tabeli konto" id="exancge-an">
        <!-- Walidacje -->
        <preConditions>
            <not>
                <tableExists tableName="konto"/>
            </not>
        </preConditions>
        <createTable tableName="konto">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="imie" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="nazwisko" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <createSequence
                sequenceName="konto_sequence"
                startValue="1"
        />
        <!-- Roll back -->
        <rollback>
            <dropTable tableName="konto"/>
        </rollback>
        <comment>Utworzono tabele 'konto'</comment>
    </changeSet>
    <changeSet author="Utworzenie tabeli saldo" id="exancge-an">
        <!-- Walidacje -->
        <preConditions>
            <not>
                <tableExists tableName="saldo"/>
            </not>
        </preConditions>
        <createTable tableName="saldo">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="waluta" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>
            <column name="saldo_konta" type="decimal(12,4)">
                <constraints nullable="false"/>
            </column>
            <column name="konto_id" type="bigint(20)" defaultValue="0">
                <constraints primaryKey="true"/>
            </column>
        </createTable>
        <!-- Roll back -->
        <rollback>
            <dropTable tableName="saldo"/>
        </rollback>
        <comment>Utworzono tabele 'saldo'</comment>
    </changeSet>

</databaseChangeLog>